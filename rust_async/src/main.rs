// 异步编程练习
// 本节目标
// 自定义实现Feature(一次异步值), Stream(可重复的异步值), Sink(一次或多次异步值)

fn main() {
    println!("Hello, world!");
    //     std::future::Future;
    //     std::stream::Stream;
    //     // std::io::Sink

    //     futures::future::Future;
    //     futures::Future;
    //     futures::Stream;
    //     futures::Sink;
}
